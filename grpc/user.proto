syntax = "proto3";

option go_package = "./grpc";

package grpc;

service UserService {
  rpc GetUser (UserRequest) returns (UserResponse) {}
}

message UserRequest {
    string id = 1;
}

message UserResponse {
    string name = 1;
    int32 age = 2;
    string id = 3;
}